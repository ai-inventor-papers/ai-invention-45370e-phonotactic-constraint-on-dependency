[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gen_art_dataset"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "datasets>=3.2.0",
    "loguru>=0.7.0",
    "huggingface-hub>=0.28.0",
]

[tool.uv]
package = false
